# Environment Variables for EcoLife Incident Reporting API

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB instance:
MONGODB_URI=mongodb://localhost:27017/ecolife_reports

# For MongoDB Atlas (cloud), use:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/ecolife_reports?retryWrites=true&w=majority

# Optional: JWT Secret (if authentication is added later)
# JWT_SECRET=your-super-secret-jwt-key-here

# Optional: Admin API Key (for additional security)
# ADMIN_API_KEY=your-admin-api-key-here
